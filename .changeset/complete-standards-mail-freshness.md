---
'unwired-mail': patch
---

Keep Standards-Based Mail fresh through revision-aware SwiftMail IDLE reconnects, foreground reconciliation, and opportunistic background polling without adding a product-owned IMAP or SMTP protocol fallback.

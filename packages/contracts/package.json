{
  "name": "@private-email/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./health": "./src/health.ts",
    "./productAccount": "./src/productAccount.ts",
    "./productSync": "./src/productSync.ts",
    "./pushRelay": "./src/pushRelay.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "format": "oxfmt --ignore-path ../../.gitignore --check .",
    "format:write": "oxfmt --ignore-path ../../.gitignore --write .",
    "lint": "oxlint --type-aware --type-check --deny-warnings --report-unused-disable-directives .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "convex": "catalog:"
  },
  "devDependencies": {
    "@rajzik/tsconfig": "catalog:",
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
